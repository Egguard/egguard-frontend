import UserContainer from "../components/.containers/UserContainer";

const User = () => {
  return (
    <div className="h-screen mt-30 flex justify-center">
      <UserContainer />
    </div>
  );
};

export default User;
